# next-auth v5 declares an OPTIONAL peer on nodemailer@^7 (for its Email provider,
# which we don't use — we use the Credentials provider). We depend on nodemailer@9
# for the quote form, so npm's strict peer resolution errors out.
# This keeps `npm install` AND `npm ci` (used by the deploy workflow) working.
legacy-peer-deps=true
